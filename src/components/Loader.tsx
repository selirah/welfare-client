import { PuffLoader } from 'react-spinners'

const Loader = () => {
  return (
    <div style={{ textAlign: 'center', marginTop: '10rem' }}>
      <PuffLoader color="#7367f0" />
    </div>
  )
}

export default Loader
